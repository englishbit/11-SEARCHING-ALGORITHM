#include<iostream>
using namespace std;
#define max 100000 
int array[max];
int ternary_search(int array[],int l,int r,int target){
    while(l<=r){
       int mid1=l+(r-l)/3;
       int mid2=r-(r-l)/3;

       if(array[mid1]==target)return mid1;
       if(array[mid2]==target)return mid2;

      if(target<array[mid1]){
          r=mid1-1;
      }else if(target>array[mid2]){
          l=mid2+1;
      }else{
          l=mid1+1;
          r=mid2-1;
        }
    }
    return -1;
}
int main(){
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>array[i];
    }
    int target;
    cin>>target;
    if(ternary_search(array,0,n-1,target)!=-1){cout<<"found";}
    else{cout<<"not found";}
    return 0;

}