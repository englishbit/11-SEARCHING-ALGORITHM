#include<iostream>
using namespace std;
#define MAX 100000
int array[MAX];
//logic for binary search
int binary_search(int array[],int l,int r,int target){
    while(l<=r){
        int mid=(l+r)/2;
        if(array[mid]==target)return mid;
        else if(target<array[mid]){r=mid-1;}
        else{l=mid+1;}
    }
    
    return -1;
}

int main(){
    int n;
    //logic for taking input
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>array[i];
    }


    int target;
    cin>>target;

    //logic for output
    if(binary_search(array,0,n-1,target)!=-1)
    cout<<"found"<<endl;
    else
    cout<<"not found"<<endl;
    
    return 0;

}


